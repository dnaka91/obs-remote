version: v1
build:
  excludes: [crates/api-legacy]
breaking:
  use:
    - FILE
lint:
  use:
    - DEFAULT
  except:
    - DIRECTORY_SAME_PACKAGE
    - PACKAGE_DIRECTORY_MATCH
    - PACKAGE_VERSION_SUFFIX
